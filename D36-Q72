Q72-Find the sum of all elements in a matrix.
Input 1:
2 3
1 2 3
4 5 6
Output 1:
21

CODE-
#include <stdio.h>

int main() {
    int rows, cols;
    scanf("%d %d", &rows, &cols);

    int mat[rows][cols];
    int sum = 0;

    // Read matrix and add to sum
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            scanf("%d", &mat[i][j]);
            sum += mat[i][j];
        }
    }

    printf("%d\n", sum);

    return 0;
}
