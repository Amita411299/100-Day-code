Q97-Print the initials of a name.
Input 1:
John Doe
Output 1:
J.D.
CODE-
#include <stdio.h>

int main() {
    char str[200];
    fgets(str, sizeof(str), stdin);

    // Print the first initial (skip spaces at start)
    int i = 0;
    while (str[i] == ' ') i++;

    if (str[i] != '\0') {
        printf("%c.", str[i] >= 'a' && str[i] <= 'z' ? str[i] - 32 : str[i]);
    }

    // Print initials of remaining words
    for (; str[i] != '\0'; i++) {
        if (str[i] == ' ' && str[i + 1] != ' ' && str[i + 1] != '\0' && str[i + 1] != '\n') {
            char ch = str[i + 1];
            if (ch >= 'a' && ch <= 'z') ch -= 32;  // convert to uppercase
            printf("%c.", ch);
        }
    }

    return 0;
}
