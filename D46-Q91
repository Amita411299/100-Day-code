Q91-Remove all vowels from a string.
Input 1:
education
Output 1:
dctn
CODE-
#include <stdio.h>

int main() {
    char str[200], result[200];
    int i = 0, j = 0;

    // Read string
    fgets(str, sizeof(str), stdin);

    while (str[i] != '\0') {
        char ch = str[i];

        // Check if vowel
        if (!(ch=='a'||ch=='e'||ch=='i'||ch=='o'||ch=='u'||
              ch=='A'||ch=='E'||ch=='I'||ch=='O'||ch=='U')) {
            result[j] = ch;  // keep non-vowel
            j++;
        }
        i++;
    }

    result[j] = '\0';  // end string

    printf("%s", result);

    return 0;
}
