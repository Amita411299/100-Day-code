Q55-Write a program to print all the prime numbers from 1 to n.
Input 1:
10
Output 1:
2 3 5 7
Input 2:
20
Output 2:
2 3 5 7 11 13 17 19
CODE-
#include <stdio.h>

int isPrime(int x) {
    if (x < 2) return 0;
    for (int i = 2; i*i <= x; i++) {
        if (x % i == 0)
            return 0;
    }
    return 1;
}

int main() {
    int n;
    scanf("%d", &n);

    for (int i = 1; i <= n; i++) {
        if (isPrime(i))
            printf("%d ", i);
    }

    return 0;
}
