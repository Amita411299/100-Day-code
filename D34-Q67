Q67-Insert an element in an array at a given position.
Input 1:
4
10 20 30 40
2 15
Output 1:
10 20 15 30 40
CODE-
#include <stdio.h>

int main() {
    int n, pos, x;
    scanf("%d", &n);

    int arr[n + 1];  // extra space for new element

    for (int i = 0; i < n; i++)
        scanf("%d", &arr[i]);

    scanf("%d %d", &pos, &x);  // pos = position, x = element to insert

    // Shift elements to the right
    for (int i = n; i >= pos; i--) {
        arr[i] = arr[i - 1];
    }

    arr[pos - 1] = x;  // insert at (pos-1) since array is 0-indexed

    // Print array
    for (int i = 0; i <= n; i++)
        printf("%d ", arr[i]);

    return 0;
}
