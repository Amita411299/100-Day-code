Q98-Print initials of a name with the surname displayed in full.
Input 1:
John David Doe
Output 1:
J.D. Doe
CODE-
#include <stdio.h>
#include <string.h>

int main() {
    char str[200];
    fgets(str, sizeof(str), stdin);

    int len = strlen(str);

    // Remove trailing newline from fgets
    if (str[len - 1] == '\n')
        str[len - 1] = '\0';

    // Find the start index of the surname (last word)
    int i = len - 1;
    while (i >= 0 && str[i] == ' ')
        i--; // skip trailing spaces

    // Move back to find start of surname
    int end = i;
    while (i >= 0 && str[i] != ' ')
        i--;

    int surname_start = i + 1;

    // Print initials of all first/middle names
    i = 0;
    while (i < surname_start) {
        if ((i == 0 && str[i] != ' ') || 
            (str[i] == ' ' && str[i + 1] != ' ' && str[i + 1] != '\0')) {

            char ch = str[i == 0 ? i : i + 1];

            // Convert to uppercase if lowercase
            if (ch >= 'a' && ch <= 'z')
                ch -= 32;

            printf("%c.", ch);
        }
        i++;
    }

    // Print surname in full
    printf(" %s", &str[surname_start]);

    return 0;
}
