Q96-Reverse each word in a sentence without changing the word order.
Input 1:
I love coding
Output 1:
I evol gnidoc
CODE-
#include <stdio.h>
#include <string.h>

void reverse(char *start, char *end) {
    while (start < end) {
        char temp = *start;
        *start = *end;
        *end = temp;
        start++;
        end--;
    }
}

int main() {
    char str[200];
    fgets(str, sizeof(str), stdin);

    int n = strlen(str);
    int i = 0, word_start = 0;

    while (i <= n) {
        if (str[i] == ' ' || str[i] == '\0' || str[i] == '\n') {
            reverse(str + word_start, str + i - 1);
            word_start = i + 1;
        }
        i++;
    }

    printf("%s", str);
    return 0;
}
