Q79-Perform diagonal traversal of a matrix.
Input 1:
3 3
1 2 3
4 5 6
7 8 9
Output 1:
1 2 4 7 5 3 6 8 9
CODE-
#include <stdio.h>

int main() {
    int n, m;
    scanf("%d %d", &n, &m);

    int a[n][m];

    // Input matrix
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            scanf("%d", &a[i][j]);
        }
    }

    // Diagonal traversal
    for (int d = 0; d < n + m - 1; d++) {

        int r, c;

        if (d % 2 == 0) {
            // Even diagonal → upward direction
            r = (d < n) ? d : n - 1;
            c = d - r;

            while (r >= 0 && c < m) {
                printf("%d ", a[r][c]);
                r--;
                c++;
            }
        } else {
            // Odd diagonal → downward direction
            c = (d < m) ? d : m - 1;
            r = d - c;

            while (c >= 0 && r < n) {
                printf("%d ", a[r][c]);
                r++;
                c--;
            }
        }
    }

    return 0;
}
